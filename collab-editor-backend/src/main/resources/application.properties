# 应用基本信息
spring.application.name=collab-editor-backend
spring.profiles.active=dev

# MySQL??
spring.datasource.url=jdbc:mysql://localhost:3306/collab_editor?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=123Songhanbin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Redis??
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.password=
spring.redis.timeout=3000ms

# MyBatis-Plus?????????????????
mybatis-plus.type-aliases-package=com.collab.collab_editor_backend.entity
mybatis-plus.configuration.map-underscore-to-camel-case=true
mybatis-plus.global-config.db-config.id-type=auto
mybatis-plus.global-config.db-config.table-prefix=t_
mybatis-plus.mapper-locations=classpath:mapper/*.xml
# 开发环境开启自动建表
mybatis-plus.global-config.db-config.auto-create-table=true
# MinIO??
minio.endpoint=http://localhost:9000  
minio.accessKey=minioadmin
minio.secretKey=minioadmin
minio.bucketName=collab-editor

# JWT配置
jwt.secret=mUpWEUth84cpntS3ybhP12RFQB8hI3hh643/QEYDeOI=
jwt.expiration=86400000
jwt.header=Authorization

# ????
# 启用spring.sql.init，以便执行schema.sql创建缺失的表
spring.sql.init.mode=always
server.tomcat.uri-encoding=UTF-8
spring.http.encoding.force=true
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true

# HTTP配置
server.ssl.enabled=false
server.port=8080

# Jackson配置
spring.jackson.default-property-inclusion=NON_NULL
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=Asia/Shanghai
spring.jackson.encoding=UTF-8

# 日志配置
logging.level.root=INFO
logging.level.com.collab=DEBUG
logging.file.name=logs/collab-editor.log
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n
# 关闭Tomcat的Access日志
server.tomcat.accesslog.enabled=false

# Agora 配置
agora.app-id=28cc827006c549dfacd541785bd2284f
agora.certificate=
agora.token-expiration=3600

# Actuator 配置
management.endpoints.web.exposure.include=health,info,metrics,loggers
management.endpoint.health.show-details=always
management.metrics.tags.application=${spring.application.name}